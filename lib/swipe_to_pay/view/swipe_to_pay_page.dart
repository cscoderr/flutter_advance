import 'package:flutter/material.dart';

class SwipeToPayPage extends StatelessWidget {
  const SwipeToPayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
